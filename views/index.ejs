<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Show All Cars</title>
</head>
<body>
    <a href="/">Back to Home Page</a>
    <a href="/cars/new">Add a Car</a>
    <h1>All Your Favorite Cars</h1>
    <ul>
        <% cars.forEach(car => {  %>
            <li>
                <a href="/cars/<%= car._id %>">
                <%= car.name %>
                </a>
                <ul>
                    <li>Make: <%= car.make %></li>
                    <li>Model: <%= car.model %></li>
                    <li>Year: <%= car.year %></li>
                    <li>Color: <%= car.color %></li>
                    <li>Good for Kids?: <%= car.isPerfectforYOU %></li>  
                    <li><img src="<%= car.image %>" alt="<%= car.name %>"></li>     
                </ul>                
            </li>
        <%  }) %>
    </ul>
</body>
</html>